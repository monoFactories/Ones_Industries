package mod.base.modification.repositories.sounds;

import core.management.DualRepository;

public class SoundRepository extends DualRepository<MediaTag> {
    public static final SoundRepository sounds = new SoundRepository();
}
