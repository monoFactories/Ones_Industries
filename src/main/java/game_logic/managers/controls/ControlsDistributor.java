package game_logic.managers.controls;

import core.moding.data.ControlRegister;

public class ControlsDistributor {
    public static void distribute (ControlRegister constantControls, String modName) {
        //Debug.debug("start distributing controls from mod: " + modName);
        //ControlModManager.controls.addEntry(modName, CurrentModsController.fromConstant(constantControls));
        //DualRepository.SingleRepository<Consumer<ControlContext>> entry = new DualRepository.SingleRepository<>();
        //constantControls.getVariables().forEach(constantVar -> {
        //    entry.add(constantVar.id(), constantVar.getActionOnLaunch());
        //});
        //ControlsExecutor.executor.addEntry(modName, entry);
        //Debug.debug("end distributing controls from mod: " + modName);
    }
}
